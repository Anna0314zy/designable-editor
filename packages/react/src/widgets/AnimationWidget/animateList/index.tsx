import React, { FC } from "react";
import { observer } from '@slides/reactive-react';

interface AnimateListProps {
}

export const AnimateList: FC<AnimateListProps> = observer(() => {
    return (
        <>
            
        </>
    )
});
