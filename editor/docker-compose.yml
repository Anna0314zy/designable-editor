services:
  editor:
    build:
      context: ..
      dockerfile: editor/Dockerfile
      args:
        NODE_IMAGE: node:20
        NGINX_IMAGE: nginx:stable
    environment:
      API_UPSTREAM: http://8.141.7.113:5177
    ports:
      - "8080:80"
